export enum Roles {
    USER = 0,
    ADMIN = 1
}

export enum Status {
    OPEN = 0,
    PENDING = 1,
    PROCESSED = 2
}

export enum Action {
    SUBMITPHOTOS = 0,
    SUBMITRESULT = 1
}

export enum FileType {
    DXF,
    SKP
}
